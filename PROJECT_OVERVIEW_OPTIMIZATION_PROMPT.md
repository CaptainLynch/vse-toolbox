# VSE Toolbox 项目状态概览页优化提示词

你是一名资深前端工程师和企业级 UI/UX 设计师。请基于当前 VSE Toolbox 仓库的实际代码，优化 Web 端“概览”页面，将其改造成面向工程项目管理的“项目状态仪表盘”。必须严格执行“先提交计划、确认后实施”的两阶段流程；当前提示词本身不构成实施授权。

本任务不是重新设计整个应用，也不是创建营销页。目标是在保持现有导航、主题和业务功能稳定的前提下，让“概览”页清楚回答四个问题：

1. 当前项目处于哪个阶段；
2. 当前阶段有哪些关键里程碑；
3. 五项核心交付物分别完成到什么程度；
4. 当前最重要的计划偏差和风险是什么。

## 0. 强制执行流程：先计划，后实施

### 第一阶段：只分析并生成计划

第一阶段只允许读取仓库、搜索代码、运行只读检查，并新建或更新 `PROJECT_OVERVIEW_IMPLEMENTATION_PLAN.md`。不得修改任何应用代码、样式、模板、测试、接口、数据库或业务数据，也不得启动自动实施。

计划必须基于实际代码证据，至少包含：

1. 当前概览页、交付物明细、数据来源、渲染流程和事件绑定的现状；
2. `状态总览`完全只读的边界，以及需要删除或避免的编辑入口；
3. `交付物明细`的查看、关联、编辑、保存和取消流程；
4. 字段矩阵：每个字段是否可编辑、校验规则、数据来源和保存目标；
5. 关联模型：可关联对象、关系类型、候选数据来源、去重规则、删除规则和展示方式；
6. 当前仓库是否已有可复用的写入接口或持久化机制，并给出代码位置；
7. 如果没有现成写入能力，分别说明“仅当前会话草稿”和“新增持久化写入”两种方案的影响，并明确推荐方案；不得自行选择并偷偷实现；
8. 拟修改文件、关键 symbol、分步实施顺序、测试方案、兼容性风险和回滚方式；
9. 所有需要用户确认的产品或架构决策。

第一阶段输出计划后，只汇报计划文件路径、核心方案和待确认项，然后明确写出“等待确认，尚未修改项目代码”，立即停止。不得在同一轮继续实施。

### 第二阶段：收到明确确认后再实施

只有用户明确回复“确认实施”、明确批准某一版计划，或给出等价的清晰授权后，才可进入第二阶段。模糊回复、补充需求或仅要求查看计划都不视为授权。

实施时只能执行已确认计划中的内容。若实际代码与计划冲突，或需要新增接口、修改数据库、改变公共契约，应先更新计划并再次等待确认，不得擅自扩大范围。

## 1. 开始前必须完成

先阅读并理解以下文件，不要根据想象重建页面：

- `AGENTS.md`
- `web/templates/dashboard.html`
- `web/static/style.css`
- `web/static/app.js`
- `web/app.py`
- `core/config.py`
- `DESIGN.md`
- `FRONTEND_REDESIGN_EXECUTION_PLAN.md`
- 与 Web 静态结构和交付物页面相关的现有测试

然后执行 `git status --short` 和相关文件的 scoped diff，识别当前工作区已有修改。工作区不是干净的：只做与本任务直接相关的增量修改，不得覆盖、回滚或整理无关变更。

## 2. 技术与修改边界

当前 Web 前端为 Flask 模板 + 原生 HTML/CSS/JavaScript。必须沿用现有技术栈和组件模式。

第二阶段经确认后允许修改：

- `web/templates/dashboard.html` 中的概览区域；
- `web/static/style.css` 中与新概览组件有关的样式；
- `web/static/app.js` 中概览渲染、页签切换和明细展开逻辑；
- 与概览静态结构、交互和回归保护直接相关的测试。

未经计划明确说明且未经用户确认，禁止修改：

- Flask API 路由及响应结构；
- 数据库结构和数据访问逻辑；
- `core/config.py`；
- Aras 页面及其请求、认证、导出逻辑；
- 顶部导航中的现有“交付物”工作台及其目录、查询和导出逻辑；
- 其他服务层或 CLI 代码。

不得引入 React、Vue、TypeScript、Tailwind、Bootstrap、外部 CDN、在线字体或大型图表库。不得连接飞书或内网。只有在计划证明现有写入能力无法满足需求、列明接口契约和影响，并获得用户单独确认后，才允许新增后端写入能力。

第一阶段必须确认项目状态数据目前来自本地模拟数据、现有 API 还是其他来源，不能沿用未经验证的假设。第二阶段应使用一个明确的数据源，不要将同一组数据分别硬编码进 HTML、CSS 和多个函数。现有 `/api/overview` 默认保持不变，以免破坏公共契约；任何变更都必须先列入计划并单独确认。新页面无需展示原来的飞书同步卡片。

## 3. 与现有“交付物”页面的边界

主导航中的“交付物”仍然是现有工具目录和业务操作工作台，必须完整保留。

概览内部的“交付物明细”仅处理当前 `VPI-T2` 阶段的五项项目管理交付物。它可以在已确认方案范围内提供关联和编辑能力，但不得提供查询、抓取、导出、飞书连接或内网连接入口。它与主导航“交付物”工作台不能共享会造成状态冲突的 DOM ID、业务状态或事件绑定。

## 4. 视觉方向

严格延续当前 VSE Toolbox 设计语言：

- 页面背景：`#faf9f5`
- 柔和表面：`#f5f0e8`
- 内容表面：`#efe9de`
- 分隔线：`#e6dfd8`
- 主文字：`#141413`
- 正文：`#3d3d3a`
- 次要文字：`#6c6a64`
- 主强调色：`#cc785c`
- 激活色：`#a9583e`
- 完成：`#5db872`
- 待审批：`#d4a017`
- 逾期：`#c64545`

复用现有 CSS 变量，确保浅色和深色主题都可用。新增样式不得复制两套完整主题。

设计原则：

- 安静、专业、紧凑，适合工程项目状态审阅；
- 页面按全宽信息带和清晰分区组织，不堆叠悬浮卡片；
- 圆角不超过 8px；
- 使用细边框和轻微层次，不使用重阴影；
- 不使用渐变背景、玻璃效果、装饰光斑、渐变球或大面积高饱和色块；
- coral 只用于激活状态和关键强调，不应铺满页面；
- 标题使用现有 serif fallback，正文使用现有 sans fallback，日期和元数据使用现有 mono fallback；
- 字号不要随 viewport 宽度缩放；`letter-spacing` 保持为 `0`；
- 不改变顶部品牌和主导航结构。

## 5. 页面结构

把当前“概览”区域改为两个职责明确的子视图：

1. `状态总览`，默认显示，完全只读；
2. `交付物明细`，负责查看、关联和编辑。

使用紧凑的分段页签或语义正确的 tab 控件切换，不创建新路由。页签切换不能重复绑定事件，也不能触发页面级布局跳动。

概览标题显示：

- 眉题：`项目工作台`
- 主标题：`项目状态`

删除概览中原有的项目计数、交付物流转和飞书同步三张统计卡片。飞书和内网只能作为只读“数据来源”标签出现，不能显示连接入口或同步操作。

## 6. 状态总览

该视图是纯展示层。所有文字、日期、状态、进度、负责人、风险和数据来源均不可编辑。不得出现输入框、下拉框、日期选择器、编辑/保存/删除按钮、`contenteditable`、拖拽修改或任何会改变业务数据的交互。允许的交互仅包括页签切换、必要的只读信息展开、聚焦和滚动。

状态总览只能读取已保存的项目状态。第二页成功保存后可以触发它重新渲染，但不得把任何编辑控件或编辑草稿带回第一页。

内容顺序必须为：

1. 主计划时间轴；
2. 当前阶段摘要；
3. 五项交付物完成状态；
4. 底部风险摘要。

只展示当前阶段 `VPI-T2`，不要展示 `OTS-S` 或任何未来阶段、占位轨道和预览内容。

### 6.1 主计划时间轴

标题：`VPI-T2 主计划时间轴`

辅助信息：

- `当前阶段关键节点`
- `2026-04-08 至 2026-08-30`
- 当前模拟日期：`2026-08-13`

月份刻度：

- `2026.04`
- `2026.05`
- `2026.06`
- `2026.07`
- `2026.08`

里程碑：

| 节点 | 日期 | 状态 |
|---|---|---|
| 项目启动 | 04-08 | 已达成 |
| 策略冻结 | 05-12 | 已达成 |
| 定点流程发布 | 06-18 | 已达成 |
| 设计冻结 | 07-15 | 已达成 |
| VDR 决策 | 08-15 | 当前目标节点 |
| VPI-T2 Gate | 08-30 | 计划节点 |

显示 `今天 08-13` 标记。状态表达必须同时依赖文字、形状和颜色：

- 已达成：绿色实心节点；
- 当前目标节点：琥珀色描边节点；
- 计划节点：灰色描边节点；
- 今天：coral 虚线标记。

桌面端使用横向里程碑轨道，移动端转换为纵向时间轴。时间轴只表达里程碑和日期，严禁混入交付物名称、负责人、完成率、审批状态、逾期状态或进度条。

### 6.2 当前阶段摘要

使用一条完整的横向摘要带，不拆成多张独立卡片。显示：

- 当前阶段：`VPI-T2`
- 阶段状态：`进行中`
- 总体进度：`64%`
- 已完成：`2 / 5`
- 风险 / 逾期：`1`
- 最近更新：`2026-08-13 09:42`

总体进度可配一条细进度轨道。`2 / 5` 表示已完全完成的交付物数量，不等同于总体加权进度 `64%`，界面和代码命名要避免混淆。

### 6.3 交付物完成状态

展示五项独立环形进度图，不使用聚合饼图：

| 交付物 | 完成度 | 状态 | 日期或风险 | 色彩 |
|---|---:|---|---|---|
| 子系统开发策略 | 100% | 已完成 | 实际完成 05-10 | 绿色 |
| SOR 定点流程 | 100% | 已完成 | 实际完成 06-17 | 绿色 |
| EWO 定点流程 | 72% | 进行中 | 计划完成 08-22 | coral |
| 造型 VDR 审批流程 | 90% | 待审批 | 计划完成 08-15 | 琥珀色 |
| 数模审批流程 | 82% | 已逾期 | 计划完成 08-08；逾期 5 天 | 红色 |

实现要求：

- 优先使用 CSS `conic-gradient` 和 CSS 自定义属性，不引入图表库；
- 环形图中心显示百分比；
- 每个图表提供可读的 `aria-label` 或等价文本；
- 状态不能只依赖颜色表达；
- 图表外框尺寸固定，长名称自然换行，不能造成布局跳动；
- 桌面端五列，中等宽度自适应为 `3 + 2`，移动端两列且最后一项可占满一行。

不要增加状态构成图、阶段趋势图、聚合饼图或重复统计模块。

### 6.4 底部摘要

使用一条紧凑摘要带显示：

- 整体完成：`64%`
- 计划进度：`80%`
- 偏差：`-16%`
- 风险：`数模审批流程已逾期 5 天`
- 本地数据快照：`2026-08-13 09:42`

风险使用红色文字、图标或左侧语义色标记，不使用整块高饱和红色背景。

## 7. 交付物明细

该视图处理当前 `VPI-T2` 的五项项目交付物，默认处于只读浏览状态，并在用户主动点击编辑后进入明确的编辑状态。编辑行为必须集中在第二页，不得让第一页面承担快捷编辑。

顶部阶段摘要显示：

- `VPI-T2`
- `进行中`
- `总体进度 64%`
- `2026-04-08 至 2026-08-30`

桌面端使用完整表格，列为：

- 交付物
- 状态
- 负责人
- 计划完成
- 实际完成或当前进度
- 风险与备注
- 数据来源
- 关联项
- 展开控制

数据：

| 交付物 | 状态 | 负责人 | 计划完成 | 实际完成 / 进度 | 风险与备注 | 数据来源 |
|---|---|---|---|---|---|---|
| 子系统开发策略 | 已完成 | 王晨 | 2026-05-12 | 2026-05-10 | 无 | 内网 |
| SOR 定点流程 | 已完成 | 周敏 | 2026-06-18 | 2026-06-17 | 无 | 内网 |
| EWO 定点流程 | 进行中 | 李珊 | 2026-08-22 | 72% | 按计划推进 | 飞书 |
| 造型 VDR 审批流程 | 待审批 | 陈璇 | 2026-08-15 | 90% | 等待设计总监审批 | 内网 |
| 数模审批流程 | 已逾期 | 赵岩 | 2026-08-08 | 82% | 逾期 5 天 | 内网 |

点击整行或末列的熟悉展开图标时，在该行下方显示内联详情，不使用全屏弹窗。只读详情包含：

- 当前状态；
- 负责人；
- 计划完成日期；
- 实际完成日期或当前进度；
- 数据更新时间；
- 数据来源；
- 已关联对象及关系类型；
- 编辑入口。

展开控制必须维护 `aria-expanded`，支持键盘操作。移动端不要压缩桌面表格，应切换为带分隔线的纵向项目列表。

### 7.1 编辑模式

- 使用清晰的铅笔图标按钮进入编辑模式，并提供文字可读的 tooltip 和无障碍名称；
- 编辑区域放在当前行的内联详情中，不做整页编辑，也不使用全屏弹窗；
- 交付物 ID、交付物名称、所属阶段和数据来源默认只读；
- 状态、负责人、计划完成日期、实际完成日期、当前进度、风险与备注是否可编辑，必须以第一阶段确认的字段矩阵为准；
- 不允许同时对同一条记录开启多个相互冲突的编辑会话；
- 必须提供明确的“保存”和“取消”操作；取消应丢弃草稿并恢复已保存值；
- 保存前执行必填项、日期逻辑、进度范围和状态一致性校验，错误应贴近对应字段显示；
- 保存期间禁用重复提交并显示状态；失败时保留草稿、展示可操作错误，不得假装保存成功；
- 存在未保存修改时，切换记录、切换页签或离开页面应给出明确保护；
- 保存成功后退出编辑状态、刷新当前明细，并让只读状态总览从已保存数据重新渲染。

### 7.2 关联内容

在内联详情中增加“关联内容”区域。关联的业务含义和候选范围必须在第一阶段根据仓库现有模型确定并提交确认，不得凭空创造真实业务实体。

经确认后的关联功能至少应满足：

- 展示对象类型、名称或编号、关系类型和来源；
- 可从合法候选集合中添加关联，不以任意自由文本伪造对象 ID；
- 同一对象和关系类型不得重复关联；
- 可以移除允许解除的关联，并对破坏性操作进行清晰确认；
- 添加或移除关联与普通字段编辑使用一致的草稿、保存、取消和失败恢复规则；
- 空状态、无候选项、加载失败和权限不足均有明确反馈；
- 不因“关联”功能连接飞书、内网或其他外部服务，也不复用主导航“交付物”工作台的查询和导出流程。

### 7.3 数据与持久化约束

编辑后的数据必须有经过确认的保存语义。不得用按钮反馈、DOM 临时更新或刷新即丢失的状态冒充持久化成功。

- 如果复用现有写入接口，计划中必须给出路由、请求结构、响应结构、错误处理和调用位置；
- 如果采用仅当前会话草稿，界面必须明确标识“未持久化”，且必须由用户在计划阶段明确选择；
- 不得擅自使用 `localStorage`、IndexedDB、文件写入、新 API 或数据库迁移；
- 不得把修改同步到飞书、内网或 Aras；
- 状态总览与交付物明细必须读取同一份已保存数据，避免双份状态漂移。

## 8. JavaScript 实现要求（仅第二阶段）

- 使用单一、集中的已保存状态和独立的编辑草稿；不得直接在输入事件中污染已保存状态；
- 使用 `textContent`、`createElement`、`append` 等安全 DOM API 渲染数据；
- 不把未经转义的数据直接拼接进 `innerHTML`；
- 页签、行展开、编辑和关联事件只绑定一次；
- 保留加载、空数据、保存中、保存成功、校验失败和请求失败状态的显示能力；
- 未经确认不使用 `localStorage` 或其他新增持久化机制保存项目业务数据；现有主题 key 除外；
- 不改变 Aras 和现有交付物工作台的状态变量、请求载荷、认证清理或事件流程；
- 不输出到 `console.log`；
- 函数和类名使用清晰语义，例如 `setupOverviewTabs`、`renderProjectOverview`、`renderDeliverableDetails`、`startDeliverableEdit`、`saveDeliverableChanges`。

建议新增的语义类名：

- `.project-overview`
- `.overview-switch`
- `.milestone-timeline`
- `.milestone-node`
- `.phase-summary`
- `.deliverable-progress-grid`
- `.progress-ring`
- `.deliverable-detail-row`

不要为了复用而建立复杂抽象；只添加能减少真实重复或稳定交互状态的辅助函数。

## 9. 响应式与可访问性

桌面端 `1440×1024`：

- 延续约 1200px 内容宽度；
- 时间轴横向展示；
- 五个环形图单行等宽排列；
- 首屏尽量包含时间轴、阶段摘要和主要交付物状态。

中等宽度 `1024px`：

- 环形图为 `3 + 2` 或稳定的自适应网格；
- 时间轴保持横向，可减少辅助文字；
- 不出现页面级横向滚动。

移动端 `390×844`：

- 信息顺序保持为页签、纵向时间轴、阶段摘要、环形图、风险摘要；
- 环形图两列，最后一项可占满一行；
- 明细表转换为纵向列表；
- 触控目标不小于约 40px；
- 中文不得溢出、重叠或无意义截断。

可访问性要求：

- 页签使用 `role="tablist"`、`role="tab"`、`role="tabpanel"`、`aria-selected` 和 `aria-controls`；
- 支持鼠标和键盘切换页签；
- 状态不能只用颜色表达；
- 进度图有文字百分比和可读标签；
- 展开按钮使用 `aria-expanded` 和清晰的无障碍名称；
- 保持浅色、深色主题下足够的文字对比度；
- 尊重现有 `prefers-reduced-motion` 规则。

## 10. 验证与交付

第一阶段只验证计划完整性，不运行会改变项目状态的命令。计划必须逐项回答：总览为何保证只读、明细哪些字段可编辑、关联对象来自哪里、数据如何保存、哪些决策尚待用户确认。

第二阶段完成后必须执行并报告：

1. JavaScript 语法检查：`node --check web/static/app.js`；
2. Flask/Python 编译检查：`python -m py_compile web/app.py`；
3. 与 Web、Aras、交付物相关的 focused tests；
4. Flask test client 对 `/` 和现有关键 API 的加载/契约验证；
5. `1440×1024`、`1024px`、`390×844` 三种 viewport 截图；
6. 每种 viewport 的浅色和深色主题检查；
7. 检查中文溢出、重叠、截断和页面级横向滚动；
8. 检查主导航 Aras 和“交付物”工作台没有回归；
9. 检查浏览器控制台无新增错误；
10. 最终检查 `git diff --stat` 和 `git status --short`，确认没有触碰任务外文件。
11. 检查状态总览不存在输入框、编辑按钮、`contenteditable` 或任何数据写入路径；
12. 检查明细页的编辑、保存、取消、校验失败、保存失败和未保存离开保护；
13. 检查关联项的添加、去重、移除、空状态和失败恢复；
14. 刷新页面验证已确认的持久化语义，确保界面没有虚假保存；
15. 保存明细后检查状态总览只读数据同步刷新且无编辑草稿泄漏。

视觉验证不能只看 DOM。必须实际截图并检查：

- 时间轴节点和今天标记是否错位；
- 五个环图是否尺寸稳定；
- 长中文名称是否挤压或遮挡；
- 深色主题是否仍有足够对比度；
- 移动端表格是否正确转换为列表；
- 明细编辑器和关联控件是否溢出、遮挡或导致布局跳动；
- 保存、取消、错误和未保存状态是否清晰；
- 页面是否出现不必要的横向滚动。

较长测试、构建或浏览器日志写入 `.runtime/`，最终报告只包含：

1. 修改文件；
2. 每个文件的一句修改说明；
3. 测试命令和结果摘要；
4. 截图绝对路径；
5. 不超过五项剩余风险。

不要返回完整文件、完整 diff 或大段测试日志。没有完成验证时，不得宣称任务完成。

## 11. 明确禁止

- 不得跳过第一阶段，或在用户确认计划前修改项目代码；
- 不得把提示词本身、计划生成请求或模糊回复视为实施授权；
- 不得在 `状态总览` 中提供任何编辑、保存、删除、拖拽修改或快捷修改入口；
- 不得用仅修改 DOM、固定成功提示或刷新即丢失的数据冒充保存成功；
- 不得在未确认数据模型和保存方案前实现关联或编辑功能；
- 不展示 `OTS-S` 或其他未来阶段；
- 不在主计划时间轴中展示交付物信息；
- 不增加状态构成图、阶段趋势图或聚合饼图；
- 不恢复飞书同步、周报、内网连接入口；
- 不连接飞书或内网；
- 不在未经计划说明和用户单独确认的情况下修改后端接口、数据库或公共数据契约；
- 不破坏现有 Aras 和交付物工作台；
- 不引入新前端框架、外部 CDN 或在线字体；
- 不创建营销式欢迎页；
- 不大规模重构无关代码；
- 不使用 `TODO`、省略号或占位实现；
- 不覆盖工作区已有的无关修改。
