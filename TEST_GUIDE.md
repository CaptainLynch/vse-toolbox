# VSE TOOLBOX — 公司环境测试指南

> 本文档描述如何在公司电脑（无 Python、无 pip、无管理员权限）上部署和测试工具箱。
> 每完成一轮测试后，记录结果到「测试记录」表格。

---

## 1. 测试环境信息

| 项目 | 说明 |
|------|------|
| 目标电脑 | 公司办公电脑 |
| 操作系统 | Windows 10/11 |
| Python 环境 | ❌ 无 |
| pip 能力 | ❌ 无 |
| 管理员权限 | ❌ 无 |
| VSCode | ✅ 已安装（无扩展） |
| 浏览器 | Chrome（内网）+ Edge（外网白名单） |
| 网络 | 内网全通，外网仅 Edge 白名单 |
| 文件加密 | ✅ 自动加密已启用 |
| GAC 限制 | ✅ 需规避 |

---

## 2. 部署方式对比

| 方式 | 难度 | 前提条件 | 推荐度 |
|------|------|----------|--------|
| **A: 打包 exe（推荐）** | ⭐ | 家中完成打包 | ⭐⭐⭐⭐⭐ |
| **B: VSCode + 离线依赖** | ⭐⭐⭐ | 家中导出 site-packages | ⭐⭐⭐ |
| **C: Docker 容器** | ⭐⭐ | 公司允许 Docker Desktop | ⭐⭐ |

### 方式 A：打包 exe（首选）

**家中准备步骤：**

```bash
# 1. 确保前后端都已完成开发
# 2. 前端构建
cd frontend && npm run build
# 3. 将 dist/ 复制到 backend/dist/
cp -r frontend/dist backend/dist

# 4. 安装 PyInstaller
pip install pyinstaller

# 5. 执行打包脚本
cd backend
python build.py
```

**打包输出：**
```
dist/
  VSE_TOOLBOX.exe          # 单文件可执行程序（约 150-200MB）
```

**公司部署步骤：**

1. 将 `VSE_TOOLBOX.exe` 通过 **公司内部文件传输** 或 **U盘** 拷贝到电脑
2. 在用户目录下创建工具箱文件夹：`C:/Users/{工号}/VSE_TOOLBOX/`
3. 将 exe 放入该文件夹
4. 从家中拷贝 `drivers/` 目录（chromedriver.exe + msedgedriver.exe）到同一文件夹
5. 从家中拷贝 `templates/` 目录到同一文件夹
6. **双击运行** `VSE_TOOLBOX.exe`
7. 浏览器访问 `http://127.0.0.1:8002`

**目录结构（公司电脑）：**
```
C:/Users/{工号}/VSE_TOOLBOX/
├── VSE_TOOLBOX.exe
├── drivers/
│   ├── chromedriver.exe
│   └── msedgedriver.exe
├── templates/
│   ├── master/
│   ├── config/
│   └── output/
├── data/                    # 运行时自动创建
├── temp/                    # 运行时自动创建
└── logs/                    # 运行时自动创建
```

---

### 方式 B：VSCode + 离线依赖（调试用）

**家中准备步骤：**

```bash
# 1. 创建虚拟环境
python -m venv venv

# 2. 安装所有依赖
pip install -r requirements.txt

# 3. 导出 site-packages
cd venv/Lib/site-packages
zip -r ../../../site-packages.zip . -x "pip*" "setuptools*" "__pycache__"

# 4. 连同源码一起拷贝到公司
```

**公司部署步骤：**

1. 拷贝源码文件夹到公司电脑
2. 在源码目录解压 `site-packages.zip`
3. VSCode 设置 Python 解释器指向 `python.exe`
4. 终端运行 `python backend/main.py`

---

### 方式 C：Docker（如公司允许）

**家中准备步骤：**

```bash
# 1. 编写 Dockerfile
# 2. 构建镜像
docker build -t vse-toolbox .
# 3. 导出镜像
docker save vse-toolbox > vse-toolbox.tar
```

**公司部署步骤：**

1. 拷贝 `vse-toolbox.tar` 到公司电脑
2. `docker load < vse-toolbox.tar`
3. `docker run -p 8000:8000 vse-toolbox`

---

## 3. 测试用例

### 第一轮：基础功能测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-01 | exe 启动 | 双击 VSE_TOOLBOX.exe | 无 UAC 弹窗，黑窗口显示 uvicorn 启动信息 | | TODO |
| T-02 | 前端页面加载 | 浏览器访问 127.0.0.1:8002 | 显示登录/主界面，无报错 | | TODO |
| T-03 | 问题列表加载 | 打开 Dashboard 页面 | 显示问题表格，有数据 | | TODO |
| T-04 | 创建问题 | 点击新建问题单，填写提交 | 新增问题出现在列表顶部 | | TODO |
| T-05 | 更新问题状态 | 将问题状态改为 resolved | 状态列正确更新 | | TODO |
| T-06 | 删除问题 | 点击删除按钮确认 | 问题从列表移除 | | TODO |
| T-07 | KPI 统计 | 查看顶部统计卡片 | 数字与数据库一致 | | TODO |

### 第二轮：Excel 工具测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-08 | Excel 多文件合并 | 选择 3 个 Excel 文件上传 | 生成合并文件，包含多个 Sheet | | TODO |
| T-09 | 同结构拼接 | 上传 3 个同结构表格 | 纵向拼接为一个表格 | | TODO |
| T-10 | 批量改名 | 输入路径和正则表达式 | 文件按规则重命名 | | TODO |
| T-11 | 文件加密验证 | 将生成的 Excel 复制到 U 盘 | 在家用电脑无法打开（加密生效） | | TODO |
| T-12 | 大文件处理 | 上传 10MB+ 的 Excel | 正常处理，无内存溢出 | | TODO |

### 第三轮：PPT 生成测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-13 | 生成周报 PPT | 点击导出周报 | 下载 .pptx 文件 | | TODO |
| T-14 | PPT 内容验证 | 打开生成的 PPT | 包含问题统计、图表、问题清单 | | TODO |
| T-15 | 生成交付物报告 | 选择交付物生成报告 | 下载正确的交付物报告 | | TODO |
| T-16 | PPT 加密验证 | 将 PPT 复制到 U 盘 | 在家用电脑无法打开 | | TODO |
| T-17 | 图表样式 | 检查 PPT 中的图表 | 暗色主题，琥珀金配色 | | TODO |

### 第四轮：爬虫模块测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-18 | 内网页面抓取 | 输入 EWO 系统 URL | Chrome 打开页面，返回 HTML 内容 | | TODO |
| T-19 | 表格提取 | 输入含表格的内网页面 | 正确提取表格数据为 JSON | | TODO |
| T-20 | 飞书页面抓取 | 输入飞书表格 URL | Edge 打开页面（利用白名单），返回内容 | | TODO |
| T-21 | 浏览器自动选择 | 分别输入内网 URL 和飞书 URL | 内网用 Chrome，飞书用 Edge | | TODO |

### 第五轮：飞书邮件测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-22 | 邮件同步 | 点击同步邮件按钮 | 从飞书抓取邮件到本地 | | TODO |
| T-23 | 邮件列表显示 | 查看飞书邮件助手页面 | 显示同步的邮件列表 | | TODO |
| T-24 | 待办生成 | 同步后查看待办面板 | 从邮件自动提取待办任务 | | TODO |
| T-25 | 待办状态切换 | 勾选/取消待办 | 状态正确更新 | | TODO |

### 第六轮：安全/GAC 合规测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|------|--------|----------|----------|----------|------|
| T-26 | 无 UAC 弹窗 | 双击运行 exe | 不触发管理员权限申请 | | TODO |
| T-27 | 无注册表写入 | 运行后检查注册表 | 无 VSE_TOOLBOX 相关键值 | | TODO |
| T-28 | 无 PATH 修改 | 运行后检查环境变量 | PATH 无新增条目 | | TODO |
| T-29 | 数据在用户目录 | 检查 data/ 位置 | 在 C:/Users/{工号}/VSE_TOOLBOX/data/ | | TODO |
| T-30 | 文件自动加密 | 检查生成的文件属性 | 显示为加密文件（绿色锁图标） | | TODO |

---

## 4. 常见问题排查

### Q1: 双击 exe 无反应

**排查步骤：**
1. 检查是否被杀毒软件拦截 → 添加信任
2. 检查是否有黑窗口闪退 → 命令行运行 exe 查看错误信息
3. 检查 logs/ 目录下的日志文件

### Q2: 浏览器访问 127.0.0.1:8002 拒绝连接

**排查步骤：**
1. 检查 exe 是否正常启动（看黑窗口 uvicorn 日志）
2. 检查端口是否被占用 → 修改 main.py 中的端口
3. 检查防火墙是否拦截了 127.0.0.1 → 添加例外

### Q3: WebDriver 启动失败

**排查步骤：**
1. 确认 drivers/ 目录下有 chromedriver.exe 和 msedgedriver.exe
2. 确认 Chrome 和 Edge 浏览器已安装
3. 检查 WebDriver 版本与浏览器版本是否匹配
4. 检查日志中的具体错误信息

### Q4: 飞书同步失败

**排查步骤：**
1. 确认 Edge 能正常访问飞书网页版
2. 确认网络状态条显示"外网受限"（正常）
3. 检查 logs/ 中的爬虫错误日志
4. 可能需要手动登录一次飞书保存 cookie

### Q5: 生成的文件在家用电脑能打开

**排查步骤：**
1. 确认公司文件加密策略已启用
2. 确认文件是从公司电脑直接复制的（不是从网络下载）
3. 加密可能只在特定目录生效 → 确认生成路径在公司加密范围内

---

## 5. WebDriver 版本匹配

chrome/edge 浏览器升级后，WebDriver 可能需要更新：

```bash
# 在家中电脑查看浏览器版本
Chrome:  地址栏输入 chrome://version
Edge:    地址栏输入 edge://version

# 下载对应版本的 WebDriver（家中电脑有外网）
ChromeDriver: https://chromedriver.chromium.org/downloads
EdgeDriver:   https://developer.microsoft.com/microsoft-edge/tools/webdriver/

# 下载后替换 drivers/ 目录下的 exe 文件
```

---

## 6. 测试记录

| 轮次 | 测试日期 | 测试人 | 结果摘要 | 问题记录 |
|------|----------|--------|----------|----------|
| 第一轮 | | | | |
| 第二轮 | | | | |
| 第三轮 | | | | |
| 第四轮 | | | | |
| 第五轮 | | | | |
| 第六轮 | | | | |

---

## 7. 验收标准

工具箱通过测试的最低要求：

- [ ] 双击 exe 直接运行，无 UAC 弹窗
- [ ] 前端页面正常加载和显示
- [ ] Excel 合并和改名功能正常
- [ ] PPT 生成功能正常，内容完整
- [ ] 内网爬虫通过 Chrome 正常
- [ ] 飞书同步通过 Edge 正常（如白名单已开通）
- [ ] 生成的文件在公司电脑正常打开
- [ ] 生成的文件在家用电脑无法打开（加密生效）
- [ ] 不写入注册表，不修改 PATH

---

## 8. 最后更新

- 版本：v1.0
- 创建时间：2026-05-27
- 更新记录：
