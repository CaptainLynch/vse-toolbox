name: Build Windows production-validation EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - ".github/workflows/build-windows-exe.yml"
      - "core/**"
      - "services/**"
      - "web/**"
      - "main.py"
      - "requirements.txt"
      - "setup.cfg"

permissions:
  contents: read

jobs:
  build-windows-exe:
    name: Build VSE Toolbox EXE
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install runtime and build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install "pyinstaller>=6.10,<7"

      - name: Verify packaged dependency surface
        shell: pwsh
        run: |
          @'
          import importlib

          required = [
              "rich",
              "win32com.client",
              "pythoncom",
              "pywintypes",
              "xlwings",
              "imapclient",
              "pytz",
              "selenium",
              "requests",
              "flask",
          ]

          missing = []
          for name in required:
              try:
                  importlib.import_module(name)
              except Exception as exc:
                  missing.append(f"{name}: {exc!r}")

          if missing:
              raise SystemExit("Missing runtime dependencies:\n" + "\n".join(missing))

          print("Runtime dependency smoke passed")
          '@ | python -

      - name: Run production-validation tests
        shell: pwsh
        run: |
          python -m pytest `
            tests/test_excel_toolbox.py `
            tests/test_aras_crawler.py `
            tests/test_aras_cli_web.py `
            -q `
            --basetemp "$env:RUNNER_TEMP\pytest" `
            -p no:cacheprovider
          python -m py_compile main.py web/app.py services/aras_crawler.py services/excel_toolbox.py
          node --check web/static/app.js

      - name: Build onedir EXE with PyInstaller
        shell: pwsh
        run: |
          pyinstaller `
            --noconfirm `
            --clean `
            --onedir `
            --console `
            --name VSE-Toolbox `
            --collect-submodules rich `
            --collect-submodules win32com `
            --collect-submodules xlwings `
            --collect-submodules selenium `
            --hidden-import pythoncom `
            --hidden-import pywintypes `
            --hidden-import win32timezone `
            --hidden-import win32com.client `
            --hidden-import win32com.client.gencache `
            --hidden-import requests `
            --hidden-import imapclient `
            --hidden-import pytz `
            --hidden-import flask `
            --add-data "web/templates;web/templates" `
            --add-data "web/static;web/static" `
            main.py

      - name: Prepare production-validation package
        shell: pwsh
        run: |
          $package = "dist\VSE-Toolbox"
          New-Item -ItemType Directory -Force "$package\data\output" | Out-Null
          New-Item -ItemType Directory -Force "$package\data\.backup" | Out-Null
          New-Item -ItemType Directory -Force "$package\data\templates" | Out-Null

          @"
          VSE Toolbox production-validation package
          =========================================

          Entry point:
            VSE-Toolbox.exe

          Runtime platform:
            Windows workstation with Microsoft Excel installed.

          DLP / Office output requirements:
            - Excel output is generated through xlwings and the native Excel process.
            - Excel files are saved with Excel native SaveAs(FileFormat=51).
            - Do not replace this package with pandas/openpyxl file-writer output for production validation.
            - Keep generated files under data/output or another explicit user-selected Office-managed path.

          Credential handling:
            - Aras Cookie / Authorization headers are runtime inputs only.
            - This package does not persist Cookie, token, api_key, sessionid, csrf, or Authorization values.

          Included runtime directories:
            data/output     Generated Office outputs
            data/.backup    Excel overwrite backups
            data/templates  Optional local Office templates

          Build metadata:
            Commit: $env:GITHUB_SHA
            Branch/ref: $env:GITHUB_REF_NAME
            Run: $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID
          "@ | Set-Content -Path "$package\PRODUCTION_VALIDATION_README.txt" -Encoding UTF8

      - name: Smoke generated EXE
        shell: pwsh
        run: |
          if (!(Test-Path "dist\VSE-Toolbox\VSE-Toolbox.exe")) {
            throw "VSE-Toolbox.exe was not generated"
          }
          Get-ChildItem "dist\VSE-Toolbox" | Select-Object Name, Length

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: VSE-Toolbox-windows-production-validation-${{ github.sha }}
          path: dist/VSE-Toolbox/**
          if-no-files-found: error
          retention-days: 30
