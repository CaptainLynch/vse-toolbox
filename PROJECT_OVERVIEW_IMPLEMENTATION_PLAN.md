# VSE Toolbox 项目状态概览页实施计划

## 1. 计划状态与执行边界

- 当前阶段：第二阶段已按用户确认实施并完成验证。
- 实施采用本计划推荐的专用持久化方案；现有 `/api/overview` 契约保持不变。
- 本计划基于当前工作区而不是 `HEAD` 的假设状态。工作区已有大量未提交改动，本次只做了经确认范围内的增量修改，没有覆盖或整理无关改动。
- 仓库没有足够证据确定合法关联对象和关系类型，因此本轮只交付明确的只读“尚未配置关联模型”状态，没有添加任意关联写入能力。

## 2. 当前实现与代码证据

### 2.1 概览页面静态结构

当前概览区已经具备本次目标的大部分只读骨架：

- `web/templates/dashboard.html:42-105` 已包含 `状态总览` 和 `交付物明细` 两个页签。
- `#overview-status-panel` 位于 `web/templates/dashboard.html:48-70`，其中包含时间轴、阶段摘要、五项进度和风险摘要容器。
- `#overview-details-panel` 位于 `web/templates/dashboard.html:72-104`，其中包含阶段摘要和八列表格。
- 主导航中的 Aras 区域位于 `web/templates/dashboard.html:107-234`；现有“交付物”工作台位于 `web/templates/dashboard.html:236-283`，其 `#deliverable-list`、`#deliverable-detail` 等 ID 与概览区分离。第二阶段不得改变这些区域的结构、状态或事件契约。

### 2.2 数据来源与渲染流程

- 当前项目状态数据完全来自 `web/static/app.js:1-87` 的 `OVERVIEW_MOCK_DATA`，并非 `/api/overview` 或数据库。
- `renderProjectOverview` 位于 `web/static/app.js:581-607`，依次渲染里程碑时间轴、阶段摘要、交付物环图、风险摘要、明细摘要和明细表。
- `DOMContentLoaded` 位于 `web/static/app.js:1673-1682`，按顺序初始化主题、概览页签、概览渲染、主面板、Aras 和现有交付物工作台。
- `setupOverviewTabs` 位于 `web/static/app.js:622-641`，已经支持点击、方向键、Home 和 End；事件只绑定一次。
- 明细展开由 `renderDeliverableDetails`（`web/static/app.js:539-579`）和 `toggleDeliverableDetail`（`web/static/app.js:499-537`）负责；当前仅展示只读字段，没有编辑、保存、取消或关联功能。
- 当前前端没有请求 `/api/overview`。`tests/test_overview_web.py:216` 明确保护这一现状。

### 2.3 现有 `/api/overview` 契约

- 路由位于 `web/app.py:1095-1102`，只返回项目、交付物和飞书同步数量。
- 查询由 `web/app.py:391-409` 的 `_query_overview` 完成。
- 现有响应结构由 `tests/test_overview_web.py:270-277` 锁定。
- 结论：该路由不是项目状态仪表盘的数据源，第二阶段默认不修改其路径、语义或响应结构。

### 2.4 现有持久化能力

- `web/app.py` 当前没有对项目或交付物执行 `INSERT`、`UPDATE` 或 `DELETE` 的 Web 路由。
- `core/db_manager.py:156-193` 的连接上下文支持提交、回滚、外键和 WAL，但没有面向项目状态的 CRUD 方法。
- 唯一可复用的交付物更新逻辑是 CLI 中对 `deliverables.status`、`deliverables.remark` 和 `updated_at` 的更新（`main.py:165-170`），不能直接作为浏览器写入接口。
- `web/app.py:416-417` 的 `_json_error`、`web/app.py:437-444` 的 `_request_payload` 以及现有 `{ok,data}` 响应模式可以作为新 API 的错误和响应规范，但不能视为已经存在的业务写入能力。

### 2.5 数据模型现状

- `projects`、`deliverables`、`feishu_tasks`、`ncr_details` 的建表定义分别位于 `core/db_manager.py:35-43`、`:47-59`、`:63-71`、`:75-87`。
- 现有 `deliverables` 只有名称、负责人、截止日期、状态、备注等基础字段；没有进度、实际完成日期、数据来源、展示色调或关联项字段。
- 状态约束是 `pending/in_progress/done/blocked`，与页面需要的“已完成、进行中、待审批、已逾期”不完全一致。
- 当前数据库中的交付物主要是既有 F610S/NCR 数据，并不是概览页的五项 VPI-T2 模拟数据。不能直接按名称覆盖或混用。

## 3. 页面职责与只读边界

### 3.1 状态总览

`状态总览` 必须是纯展示视图，只从同一份已保存状态读取并渲染。允许的交互只有：

- 页签切换；
- 必要的只读信息展开；
- 键盘聚焦；
- 页面滚动。

该视图中禁止出现输入框、下拉框、日期选择器、`contenteditable`、编辑/保存/删除按钮、拖拽修改以及任何业务数据写入路径。明细页保存成功后，可以重新读取已保存状态并触发 `renderProjectOverview`，但不得传入或复用编辑草稿。

### 3.2 交付物明细

`交付物明细` 默认只读浏览。用户展开某一行后查看内联详情，再通过带 `aria-label` 和 tooltip 的铅笔图标进入该记录唯一的编辑会话。编辑器保持在当前行下方，不使用全屏弹窗，不改变主导航“交付物”工作台。

计划中的状态机为：

1. `viewing`：表格/移动列表与只读内联详情。
2. `editing`：从已保存记录复制独立草稿；同一时间只允许一个活动编辑会话。
3. `saving`：禁用重复提交并显示保存状态。
4. `save_failed`：保留草稿，在字段附近或操作区显示可恢复错误。
5. `saved`：用服务端返回值替换已保存状态，退出编辑，重绘明细与只读总览。
6. `cancelled`：丢弃草稿并恢复已保存值。

切换记录、切换概览页签或离开页面前，如果存在未保存修改，必须提示用户确认；取消离开不得丢失草稿。

## 4. 字段矩阵

| 字段 | 总览 | 明细默认态 | 建议编辑性 | 校验规则 | 当前来源 | 计划保存目标 |
|---|---|---|---|---|---|---|
| 交付物 ID | 只读 | 只读 | 不可编辑 | 必须存在且服务端可识别 | 模拟数据中缺少稳定 ID | 待确认的持久化记录主键 |
| 交付物名称 | 只读 | 只读 | 不可编辑 | 非空，由服务端记录决定 | `OVERVIEW_MOCK_DATA` | 不修改 |
| 所属阶段 | 只读 | 只读 | 不可编辑 | 固定为 `VPI-T2` | `OVERVIEW_MOCK_DATA.phase` | 不修改 |
| 数据来源 | 只读 | 只读 | 不可编辑 | 仅允许服务端记录中的已知值 | `OVERVIEW_MOCK_DATA.deliverables[].source` | 不修改 |
| 状态 | 只读 | 只读 | 建议可编辑 | 必填；必须在确认后的枚举中；与进度/实际完成日期一致 | 模拟数据；数据库枚举不兼容 | 新持久化模型或经批准扩展后的模型 |
| 负责人 | 只读 | 只读 | 建议可编辑 | 必填；去除首尾空白；长度上限需确认 | 模拟数据；DB 有 `owner` | 同上 |
| 计划完成日期 | 只读 | 只读 | 建议可编辑 | ISO `YYYY-MM-DD`；不得早于阶段开始；是否允许晚于阶段结束需确认 | 模拟数据 `plannedDate`；DB 有 `due_date` | 同上 |
| 实际完成日期 | 只读 | 只读 | 建议按状态可编辑 | 完成状态必填；不得早于项目开始；非完成状态应为空 | 模拟数据混在 `progressOrDate` | 同上，需新增明确字段 |
| 当前进度 | 只读 | 只读 | 建议按状态可编辑 | 整数 0-100；完成状态必须为 100；未完成状态不得伪装为完成 | 模拟数据 `progress` | 同上，需新增字段 |
| 风险与备注 | 只读摘要 | 只读 | 建议可编辑 | 允许空值；长度上限需确认；服务端再次校验 | 模拟数据 `note`；DB 有 `remark` | 同上 |
| 更新时间 | 只读 | 只读 | 不可编辑 | 由服务端生成 | 模拟数据 `updatedAt` | 服务端时间戳 |
| 关联项 | 只读 | 只读 | 仅在模型获批后编辑 | 候选必须合法；`对象类型 + 对象 ID + 关系类型` 唯一 | 当前不存在 | 待批准的关联模型 |
| 总体进度、2/5、风险数、偏差 | 只读派生 | 只读摘要 | 不可直接编辑 | 从已保存交付物计算；`2/5` 与加权 `64%` 分开命名 | 当前为模拟常量 | 服务端或统一前端派生，需确认公式 |

任何尚未确认的长度、日期边界、状态枚举和加权公式都不得在第二阶段自行发明。

## 5. 关联模型调查与建议

当前仓库只有明确的 `deliverables.project_id -> projects.id` 多对一关系。`feishu_tasks`、`ncr_details` 和 Aras/TDC 查询结果存在于代码中，但仓库没有证明它们是 VPI-T2 交付物的合法关联对象，也没有关系类型、候选集合、去重、解除权限或展示顺序规则。

因此计划采用以下安全边界：

- 在业务模型未获确认前，只能显示“尚未配置关联模型”的只读空状态，不实现任意文本 ID、添加、删除或假关联成功。
- 用户确认后，候选项必须来自服务端允许列表，不接受自由文本伪造对象 ID。
- 唯一键建议为 `deliverable_id + object_type + object_id + relation_type`。
- 删除必须只删除关联关系，不删除目标业务对象；需要明确确认。
- 展示至少包含对象类型、名称或编号、关系类型和来源。
- 添加、移除与普通字段使用同一个草稿、保存、取消和失败恢复周期。
- 不连接飞书、内网或 Aras，不复用主导航交付物工作台的查询/导出流程。

待用户确认的合法候选对象类型和关系类型见第 12 节。

## 6. 保存方案比较与推荐

### 方案 A：仅当前会话草稿

- 优点：无需后端、数据库或公共契约变更。
- 缺点：刷新即丢失；不能满足“保存后总览从已保存数据重绘”和刷新验证；必须显著标记“未持久化”；容易被误解为虚假保存。
- 结论：不推荐作为完整交付，仅可在用户明确选择“交互原型”时采用。

### 方案 B：新增项目状态专用持久化能力（推荐）

- 新增专用读取端点和更新端点，保留现有 `/api/overview` 原样不变。
- 使用独立的项目状态存储模型，避免把当前 VPI-T2 五项数据与既有 F610S/NCR `deliverables` 记录混用。
- 总览和明细只读取同一个服务端保存结果；保存成功使用服务端返回的规范化记录更新内存，再重新读取或重绘。
- 数据库迁移、VPI-T2 初始数据、API 路径和模型名称必须单独获得用户批准后才能实施。

建议 API 契约（待确认）：

- `GET /api/project-status?phase=VPI-T2`
  - 成功：`{ "ok": true, "data": { "phase": {...}, "milestones": [...], "deliverables": [...], "summary": {...} } }`
  - 不允许由客户端传入任意外部连接信息。
- `PATCH /api/project-status/deliverables/<id>`
  - 请求只允许白名单字段：`status`、`owner`、`planned_date`、`actual_date`、`progress`、`remark`，以及在模型获批后的 `associations`。
  - 成功：`{ "ok": true, "data": { "deliverable": {...}, "project_status": {...} } }`。
  - 校验错误：HTTP 400/422，沿用 `{ "ok": false, "error": { "type", "message", "fields"? } }`。
  - 记录不存在：HTTP 404；冲突或过期版本：HTTP 409；服务端失败：HTTP 500，消息经过现有清理逻辑。
  - 建议加入版本号或 `updated_at` 条件更新，防止两个会话静默覆盖。

该契约是计划建议，不是实施授权。

## 7. 拟修改文件与关键符号

### 不涉及后端的已确认视觉/只读范围

- `web/templates/dashboard.html`：只调整概览区域的语义结构和明细内联容器；不触碰 Aras 与主导航交付物工作台。
- `web/static/style.css`：新增或收敛 `.project-overview`、`.overview-switch`、`.milestone-timeline`、`.milestone-node`、`.phase-summary`、`.deliverable-progress-grid`、`.progress-ring`、`.deliverable-detail-row` 及编辑器/关联区样式；复用现有变量和主题覆盖。
- `web/static/app.js`：保留 `setupOverviewTabs`、`renderProjectOverview`、`renderDeliverableDetails` 的职责边界；新增集中保存状态、独立草稿和内联编辑辅助函数。
- `tests/test_overview_web.py`：补充只读边界、页签、行展开、响应式和安全 DOM 保护。

### 仅在方案 B 单独获批后

- `web/app.py`：新增项目状态读取/更新路由；复用 `_request_payload`、`_json_error` 和错误清理模式，不修改 `/api/overview`。
- `core/db_manager.py` 或独立的受控数据访问模块：新增经批准的表结构和 CRUD；不得影响现有表的业务语义。
- 新的数据库/API focused tests：覆盖读取、字段白名单、校验、并发冲突、回滚和错误响应。

建议 JavaScript 符号：`loadProjectOverview`、`renderProjectOverview`、`renderDeliverableDetails`、`startDeliverableEdit`、`validateDeliverableDraft`、`saveDeliverableChanges`、`cancelDeliverableEdit`、`renderAssociations`、`addDeliverableAssociation`、`removeDeliverableAssociation`、`guardUnsavedOverviewChanges`。

## 8. 分步实施顺序

1. 用户确认第 12 节决策，并明确是否只批准前端只读范围，或同时单独批准方案 B 的 API/数据库范围。
2. 先保护当前工作区：记录 scoped diff；只编辑获批文件；不覆盖现有未提交改动。
3. 若批准方案 B，先实现并测试持久化模型、初始数据策略和读取 API；保持 `/api/overview` 不变。
4. 将 `OVERVIEW_MOCK_DATA` 替换为唯一的已确认数据源；不得在 HTML、CSS 和多个函数中重复硬编码同一数据。
5. 完成状态总览的只读渲染，维持时间轴、阶段摘要、五个独立环图和底部风险摘要的既定顺序；不得展示 OTS-S 或未来阶段。
6. 完成明细的桌面表格、移动纵向列表、行展开、`aria-expanded` 和键盘操作。
7. 完成单记录草稿、编辑/保存/取消、字段级校验、重复提交保护、失败恢复和未保存离开保护。
8. 仅在关联模型获批后实现候选加载、添加、去重、移除、空状态和错误恢复；否则保留明确只读空状态。
9. 保存成功后，用服务端已保存结果重绘明细和状态总览，验证无编辑草稿泄漏。
10. 执行第 9 节全部验证，检查 scoped diff 和工作区状态；只修复本任务造成的问题。

## 9. 测试与视觉验证

第二阶段完成后必须执行并报告：

- `node --check web/static/app.js`
- `python -m py_compile web/app.py`
- Web、Aras、现有交付物工作台和新增项目状态相关 focused tests。
- Flask test client 对 `/`、现有 `/api/overview` 和获批新 API 的成功/失败契约验证。
- 只读总览中不存在输入、编辑、保存、删除、`contenteditable` 或写入路径的断言。
- 明细页编辑、保存、取消、校验失败、请求失败、重复提交和未保存离开保护。
- 关联项添加、去重、移除、空候选、加载失败和保存失败；仅在关联功能获批后执行。
- 保存后重新加载页面，验证持久化语义；同时验证总览同步刷新且不包含草稿。
- `1440x1024`、`1024px` 和 `390x844` 三种 viewport 的浅色/深色截图。
- 截图检查时间轴节点与今天标记、环图尺寸、长中文、移动列表、编辑器/关联控件、对比度、布局跳动和页面级横向滚动。
- 浏览器控制台无新增错误；主导航 Aras 和现有“交付物”工作台无回归。
- 最终检查 `git diff --stat`、scoped diff 和 `git status --short`；长日志写入 `.runtime/`。

## 10. 兼容性风险与控制

1. 当前概览是工作区内尚未提交的模拟实现；后续修改必须保留用户已有改动并以小范围补丁完成。
2. 改变 `/api/overview` 会破坏现有测试和公共契约，因此计划明确新增专用端点而不修改它。
3. 现有数据库状态枚举和字段不足，直接复用会产生语义错配；必须先确认独立模型或迁移策略。
4. 概览明细和主导航交付物工作台位于同页，必须使用独立 DOM ID、状态对象和一次性事件绑定。
5. 若关联对象和关系类型未确认，任何可编辑关联实现都会制造不存在的业务语义，因此必须停在只读空状态。
6. 模拟数据切换到 API 会使当前“不 fetch `/api/overview`”类静态测试需要按已批准的新端点精确调整，不能宽泛删除安全断言。

## 11. 回滚方式

- 前端回滚：只回退本任务对 `dashboard.html`、`style.css`、`app.js` 和对应测试的增量提交，不回退其他工作区改动。
- API 回滚：移除新增专用路由和数据访问函数，恢复为当前模拟只读状态；现有 `/api/overview` 始终保持原契约。
- 数据库回滚：实施前备份 `data/`；迁移使用可识别的独立表/版本。回滚时只删除本功能新增结构，不修改既有项目、交付物、飞书或 NCR 数据。
- 发生跨模块回归、模型冲突或两次相同根因修复失败时，停止实施，保留证据到 `.runtime/` 并返回计划阶段重新确认。

## 12. 实施采用的确认基线

1. **实施范围**：交付只读状态总览、明细浏览、内联编辑、字段校验、保存/取消、失败恢复和未保存离开保护。
2. **保存语义**：采用项目状态专用 SQLite 表和新增 API；不使用会话草稿冒充持久化，不使用 `localStorage` 保存业务数据。
3. **数据源与初始数据**：VPI-T2 五项数据作为专用模型的幂等初始快照；新增 `GET /api/project-status`，现有 `/api/overview` 不变。
4. **字段编辑矩阵**：状态、负责人、计划完成日期、实际完成日期、当前进度、风险与备注可编辑；ID、名称、阶段和数据来源只读。
5. **状态映射**：服务端专用模型直接使用“已完成、进行中、待审批、已逾期”，并校验完成状态、100% 进度和实际完成日期的一致性。
6. **总体进度公式**：总体进度 `64%` 和计划进度 `80%` 暂作为阶段基线字段保存；完成数和风险数由交付物状态派生。后续变更加权公式需再次确认。
7. **关联模型**：本轮不实现可编辑关联；显示只读未配置状态。合法对象类型、关系类型、候选来源、解除规则和权限仍需单独确认。
8. **并发策略**：使用 `updated_at` 乐观锁；过期写入返回 HTTP 409。

## 13. 当前工作区状态

第一阶段检查时，与任务直接相关的 `web/app.py`、`web/static/app.js`、`web/static/style.css`、`web/templates/dashboard.html` 已有未提交修改；`tests/test_overview_web.py` 和 `tests/test_deliverables_web.py` 为未跟踪文件。另有 Aras、TDC、飞书、Windows HTTP、`.runtime/` 和 `artifacts/` 等无关或并行改动。本计划不回退、不覆盖这些内容。

第二阶段实际修改范围为 `core/db_manager.py`、`web/app.py`、`web/static/app.js`、`web/static/style.css`、`tests/test_overview_web.py`，并新增 `tests/test_project_status_api.py`；`dashboard.html` 的既有未提交结构未被本轮覆盖。

## 14. 变更计划：主计划轴编辑（已实施）

### 14.1 边界冲突

新增“主计划轴编辑”超出最初确认范围，并与第 3.1 节“状态总览完全只读”约束冲突。用户随后确认采用独立维护区方案，因此实施保持状态总览只读，把编辑入口放在 `交付物明细`。

推荐继续保持状态总览中的时间轴只读，把编辑入口放到 `交付物明细` 页签顶部的独立“主计划维护”区域。保存成功后，状态总览从服务端已保存数据重新渲染。这样不会让只读总览承担快捷编辑，也不会破坏现有页签职责。

不推荐方案是在状态总览时间轴节点上直接提供编辑按钮或拖拽；该方案会改变已经确认的只读产品契约、键盘模型和移动端交互，需要明确覆盖原只读约束后才可实施。

### 14.2 推荐交互

1. `交付物明细` 页签顶部增加“主计划维护”折叠区，默认只读展示六个里程碑。
2. 使用铅笔图标进入单一编辑会话，不允许同时编辑交付物和主计划。
3. 编辑字段：节点名称、日期、状态；节点类型限定为 `已达成`、`当前目标节点`、`计划节点` 对应的固定枚举。
4. 支持新增节点、调整显示顺序和删除自定义节点；系统基线节点是否允许删除需要用户确认。
5. 不使用拖拽作为唯一排序方式；提供上移/下移图标按钮及键盘可操作名称。
6. 保存、取消、字段级错误、保存失败恢复、重复提交保护、未保存离开保护与交付物编辑保持一致。
7. 保存成功后重新读取完整项目状态，同时刷新只读时间轴；编辑草稿不得进入状态总览。

### 14.3 数据与 API 变更

现有 `project_status_milestones` 已具备 `name`、`milestone_date`、`status`、`type`、`sort_order`，可以复用，不需要新表。需要新增受控写入接口，现有 `/api/overview` 仍保持不变。

建议契约：

- `PATCH /api/project-status/phases/VPI-T2/milestones`
- 请求：`{ "milestones": [{ "id", "name", "date", "status", "type", "sortOrder" }], "updatedAt": "..." }`
- 成功：`{ "ok": true, "data": { "projectStatus": {...} } }`
- 校验失败：HTTP 422，沿用字段错误结构。
- 并发冲突：使用阶段 `updated_at` 乐观锁，返回 HTTP 409。
- 禁止客户端指定其他阶段、外部数据源或任意 SQL 标识。

数据库需要为里程碑提供稳定公开 ID，并明确系统基线节点删除策略。若允许新增/删除，更新必须在单一事务内完成：校验全部草稿、检查版本、写入/更新/删除、重新编号 `sort_order`、更新阶段版本；任一步失败则整体回滚。

### 14.4 校验规则

- 名称必填，去除首尾空白，建议上限 100 字符。
- 日期必须是 ISO `YYYY-MM-DD`，且位于阶段 `2026-04-08` 至 `2026-08-30` 内。
- 同一阶段的节点名称不得重复；日期是否允许重复需要用户确认。
- 恰好一个节点可标记为 `当前目标节点`；是否必须始终存在一个需要用户确认。
- `已达成` 节点日期不得晚于当前模拟日期；`计划节点` 日期不得早于当前模拟日期。
- 排序以 `sortOrder` 为准，保存时规范化为连续整数。

### 14.5 拟修改文件

- `web/templates/dashboard.html`：仅在 `交付物明细` 内增加主计划维护容器。
- `web/static/app.js`：新增独立主计划草稿、渲染、校验、保存、取消和未保存保护；复用现有项目状态读取端点。
- `web/static/style.css`：新增紧凑维护列表、日期/状态控件和移动端纵向布局。
- `web/app.py`：新增里程碑 PATCH 路由和白名单校验。
- `core/db_manager.py`：新增事务化里程碑更新方法及阶段乐观锁。
- `tests/test_overview_web.py`、`tests/test_project_status_api.py`：增加只读总览保护、主计划编辑、排序、校验、冲突与回滚测试。

### 14.6 已确认并实施的规则

1. 状态总览保持只读，编辑入口位于 `交付物明细`。
2. 允许新增、改名、排序和删除里程碑，包括六个基线节点。
3. 允许多个节点使用同一天。
4. 始终且只能存在一个“当前目标节点”。
5. 已新增里程碑 PATCH API，并使用数据库单事务替换和阶段版本乐观锁。
