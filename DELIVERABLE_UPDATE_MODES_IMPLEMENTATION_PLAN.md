# 首页交付物明细自动/手动更新实施评估

## 1. 当前状态

本文件仅用于方案评估，尚未授权实施。本轮不修改页面、API、数据库或业务数据。

当前首页项目状态的数据链路是：

- `web/static/app.js:690-705` 通过 `GET /api/project-status?phase=VPI-T2` 读取一份已保存状态。
- `web/static/app.js:1023-1066` 的 `saveDeliverableChanges` 负责手动编辑，通过 `PATCH /api/project-status/deliverables/<id>` 保存。
- `web/app.py:1374-1388` 提供读取接口；`web/app.py:1390-1433` 提供手动更新接口，并使用 `updatedAt` 做乐观冲突检测。
- `core/db_manager.py:264-310` 仅写入状态、负责人、计划/实际日期、进度和备注；保存后同步更新阶段时间戳。
- `project_status_deliverables.source` 当前只是展示标签，并不是可执行连接配置。页面中的五项 VPI-T2 数据没有外部对象 ID、匹配规则或同步游标。

现有手动更新已经具备草稿隔离、字段校验、保存/取消、失败保留、重复提交保护和 409 冲突处理，可以继续作为统一人工修改入口。

## 2. 当前可复用的自动化能力

### 2.1 已存在但尚未接入首页状态

- 飞书邮件解析：`services/feishu_imap.py:259-309` 可将邮件任务写入 `feishu_tasks`；`:311-362` 可把未同步任务转成通用 `deliverables`。
- Aras：`web/app.py:1483-1724` 已有 EWO、PAA、NCR 的查询/抓取/导出接口。
- TDC：`web/app.py:1727-1779` 已有数模和 SOR 的查询、抓取及导出接口。
- CLI：`main.py:119-173` 可人工更新通用 `deliverables`；`main.py:204+`、`:830+`、`:967+` 可运行飞书、TDC、内网相关流程。

这些能力目前都没有写入 `project_status_deliverables`，也没有把外部记录稳定映射到 VPI-T2 五项交付物。

### 2.2 当前不存在的能力

- 没有后台调度器、任务队列或常驻同步进程。
- 没有可安全持久化的 Aras/TDC/内网登录会话；现有 Web 请求要求用户当次提供认证信息。
- 没有自动同步配置、外部对象 ID、字段映射、来源优先级、同步历史或失败重试状态。
- 飞书默认 IMAP 主机仍是示例值，且当前主菜单将飞书同步标记为暂停能力，不能直接承诺无人值守自动同步。
- 五项首页交付物与外部系统的业务对应关系未被代码证明。例如 EWO 当前页面来源写为“飞书”，但仓库同时存在 Aras EWO；SOR/数模可能对应 TDC 报表，但缺少确定的筛选键；两项“内网”数据没有已确认的可查询契约。

结论：当前可以安全实现“手动编辑”和“有明确凭据、映射后的按需同步”；不能在没有补充配置与认证策略时宣称已经具备真正的定时自动更新。

## 3. 方案比较

### 方案 A：浏览器定时刷新并直接调用外部接口

浏览器使用 `setInterval` 定时调用 Aras/TDC/飞书接口，再直接覆盖首页状态。

优点：实现表面简单，用户打开页面时即可刷新。

问题：

- 页面关闭后自动更新停止。
- 多个浏览器标签会重复执行任务。
- 认证信息必须反复进入浏览器或被持久化，违反当前凭据边界。
- 网络失败和部分成功很难可靠恢复。
- 手动修改与自动覆盖容易发生无提示丢失。

结论：不推荐。

### 方案 B：Flask 进程内后台调度

在 `create_app` 内启动线程或 APScheduler，周期调用连接器并写库。

优点：实现成本低于独立服务，页面关闭后仍可运行。

问题：

- Flask 开发服务器重启会中断任务；多进程部署可能重复启动调度器。
- 当前没有任务租约、分布式锁、重试表或安全凭据存储。
- 后台网络任务会与 Web 请求生命周期耦合，影响 Aras/TDC 现有稳定性。

结论：仅适合短期演示，不推荐作为正式方案。

### 方案 C：统一更新服务 + 独立同步运行器（推荐）

手动编辑和自动同步都调用同一个领域服务。Web 继续负责展示、人工修改和“立即同步”；独立运行器负责按计划拉取外部数据、标准化、预览差异并提交更新。

核心原则：

1. 外部连接器只产生标准化候选更新，不直接修改首页表。
2. 统一 `ProjectStatusUpdateService` 执行字段校验、来源权限、乐观锁、审计和事务写入。
3. 手动更新默认优先，不允许后台任务静默覆盖人工锁定字段。
4. 自动同步失败保留最后一次成功数据，页面明确显示“同步失败/数据可能过期”，不能清空或伪造成功。
5. 运行器与 Flask 解耦，可由 CLI、Windows Task Scheduler 或后续服务管理器按周期启动。
6. 凭据不写入 SQLite、前端或日志。无人值守认证必须单独批准 Windows Credential Manager、受控环境变量或其他企业凭据机制。

## 4. 推荐的产品模型

### 4.1 三种记录级更新模式

- `manual`：只允许人工保存，自动同步只生成差异预览，不落库。
- `automatic`：已确认映射的字段由自动来源管理；人工修改必须先切换为混合模式或创建覆盖。
- `hybrid`：自动同步未锁定字段，人工修改的字段进入手动锁定状态。

首页“交付物明细”建议增加：

- `更新方式` 列：手动、自动、混合。
- `最后成功更新`、`同步状态` 和 `数据来源`。
- 行展开区提供“手动编辑”“立即同步”“查看差异”“查看更新记录”。
- 自动模式失败时显示紧凑错误和上次成功时间；总览继续使用最后一次成功保存的数据。
- 不在只读状态总览中放置自动写入控件。

### 4.2 字段归属与冲突

建议按字段而不是整条记录处理归属，因为外部系统通常只能提供部分字段：

- 可自动候选字段：状态、实际完成日期、进度、风险备注；具体取决于连接器证据。
- 默认人工字段：负责人、计划完成日期；除非用户明确批准外部字段映射。
- 人工保存某字段后，将该字段标记为 `manual`；自动运行器跳过它并记录“因人工锁定未应用”。
- 用户可显式点击“恢复自动”解除字段锁。
- 自动同步使用记录版本或 `updated_at` 条件写入；版本变化时重新计算差异，不覆盖并发人工保存。

## 5. 建议数据模型

不建议继续把同步元数据塞入 `project_status_deliverables.source` 文本。建议新增：

### `project_status_update_bindings`

- `deliverable_id`
- `mode`: `manual|automatic|hybrid`
- `source_type`: `feishu|aras|tdc|intranet|none`
- `external_key`: 经确认的外部稳定标识
- `mapping_json`: 受版本控制的字段映射配置
- `enabled`
- `interval_minutes`
- `last_attempt_at`、`last_success_at`
- `sync_state`: `idle|running|success|failed|needs_attention`
- `last_error_type`、`last_error_message`
- `cursor` 或来源版本

### `project_status_field_authority`

- `deliverable_id`
- `field_name`
- `authority`: `manual|automatic`
- `source_type`
- `locked_at`、`updated_at`
- 唯一键：`deliverable_id + field_name`

### `project_status_update_audit`

- `deliverable_id`
- `trigger_type`: `manual|sync_now|scheduled`
- `source_type`
- `external_version`
- `proposed_changes_json`、`applied_changes_json`
- `result`: `applied|skipped|conflict|failed`
- `created_at`

审计表不得保存密码、Cookie、Authorization、完整原始响应或敏感请求头。

## 6. 建议服务与 API

### 服务边界

- 新增 `services/project_status_updates.py`：规范化更新命令、字段归属、差异计算、冲突处理、事务写入和审计。
- 新增 `services/project_status_connectors.py` 或按来源拆分适配器；第一阶段只实现已经确认外部键和字段契约的来源。
- 新增 `services/project_status_sync_runner.py`：`run_once`、租约、超时、重试和汇总结果；不在 Flask 启动时自动创建线程。
- `core/db_manager.py` 仅提供绑定、字段归属、审计和原子写入数据访问。

### API 建议

- 保留 `GET /api/project-status` 和当前手动 PATCH 路径。
- 扩展手动 PATCH：服务端记录 `trigger_type=manual`，并按实际修改字段建立人工锁。
- `GET /api/project-status/deliverables/<id>/update-policy`
- `PATCH /api/project-status/deliverables/<id>/update-policy`
- `POST /api/project-status/deliverables/<id>/sync-preview`
- `POST /api/project-status/deliverables/<id>/sync-apply`
- `POST /api/project-status/sync/run`：仅触发已配置来源的一次运行，不接收任意 URL、主机或自由请求头。
- `GET /api/project-status/updates?deliverableId=...`：返回经过清理的审计摘要。

正式定时运行优先通过独立 CLI：

```text
python main.py project-status-sync --once
```

再由 Windows Task Scheduler 调用。是否需要常驻 `--daemon` 应在一次运行稳定后再决定。

## 7. 外部来源落地可行性

| 首页交付物 | 当前展示来源 | 可复用能力 | 当前自动化结论 |
|---|---|---|---|
| 子系统开发策略 | 内网 | 只有通用内网抓取器 | 缺少页面契约和外部键，暂不能自动 |
| SOR 定点流程 | 内网 | TDC SOR 查询存在 | 需要确认是否确由 TDC 驱动及筛选键 |
| EWO 定点流程 | 飞书 | 飞书邮件解析、Aras EWO 查询都存在 | 来源冲突，需要用户确定权威系统和匹配规则 |
| 造型 VDR 审批流程 | 内网 | 未发现专用 VDR 连接器 | 暂不能自动 |
| 数模审批流程 | 内网 | TDC 数模查询存在 | 需要确认外部流程编号/项目筛选键和进度映射 |

推荐第一阶段只挑选一项契约最清晰的交付物做端到端试点。若能提供 TDC SOR 或数模的真实外部键和状态字段，优先从其中一项开始；不要一次把五项都标为自动。

## 8. 分阶段实施

### 阶段 1：双模式框架与人工优先

- 新增更新模式、字段归属和审计表。
- 把现有手动保存迁移到统一更新服务，保持 API 兼容。
- 页面增加模式、来源、最后更新时间和审计入口。
- 自动来源尚未配置时显示“未配置”，不提供虚假成功。

### 阶段 2：单来源按需同步试点

- 为一项已确认交付物实现适配器和外部键映射。
- 实现“立即同步 → 差异预览 → 应用”。
- 验证字段锁、冲突、失败恢复、敏感信息清理和重复事件幂等。

### 阶段 3：独立自动运行

- 增加 `run_once`、任务租约、重试/退避和超时。
- 使用 Windows Task Scheduler 定时运行。
- 凭据方案单独安全评审；未获批准前不得存储凭据或启用无人值守外部调用。

### 阶段 4：逐项扩展

- 每个新来源分别确认外部键、状态映射、进度算法和失败语义。
- 完成一项的测试和观测后再启用下一项。

## 9. 测试与验收

- 现有手动编辑、校验、取消、保存和 409 行为保持不变。
- `manual` 模式拒绝自动落库；`automatic` 和 `hybrid` 严格遵守字段归属。
- 同一外部事件重复处理不产生重复修改或重复业务记录。
- 人工保存与自动任务并发时不发生静默覆盖。
- 自动失败保留最后成功值，并更新失败状态和清理后的错误摘要。
- 连接器使用固定 allowlist，不接受任意 URL；日志和审计不包含密码、Cookie、Token 或原始认证头。
- Flask 重启不丢失绑定、同步状态、游标和审计。
- 桌面和移动端均能清晰区分更新方式、数据新鲜度、人工锁定字段及失败状态。
- Aras/TDC/飞书现有查询、导出和凭据清理测试无回归。
- 独立运行器使用测试数据库和假连接器验证租约、超时、重试、部分失败和回滚。

## 10. 回滚

- 通过功能开关关闭自动同步，所有记录退回 `manual`，现有手动 PATCH 保持可用。
- 自动更新使用新增表和统一服务，不改变 `/api/overview`。
- 回滚连接器或运行器时保留审计记录，不删除现有首页业务数据。
- 数据库迁移前备份 `data/`；回滚只移除本功能新增绑定/审计结构，不触碰既有项目状态记录。

## 11. 用户确认记录与剩余决策

### 11.1 已确认

1. EWO 的权威来源为 Aras，不使用飞书作为该交付物的自动权威来源。
2. SOR 定点流程对应 TDC SOR 报表；数模审批流程对应 TDC 数模报表。
3. 自动来源允许修改负责人、计划完成日期和风险备注，同时仍必须经过字段映射、差异预览、审计和人工覆盖保护。
4. 首个试点选择“数模审批流程”。
5. 当前尚不清楚各交付物在外部系统中的稳定编号、流程 ID 或筛选键。

### 11.2 由稳定标识缺失产生的实施边界

数模试点不能直接进入自动落库。第一步必须是“映射发现模式”：

- 使用现有 TDC 数模查询能力，由用户当次提供认证并输入候选筛选条件。
- 只返回经过清理的候选记录和可用于匹配的字段，不修改项目状态。
- 用户选择一条或一组候选后，保存经确认的稳定筛选条件与字段映射；不得保存凭据、Cookie 或原始敏感响应。
- 至少连续两次查询得到一致目标且没有歧义后，才允许启用“立即同步”。
- 定时自动同步必须在稳定映射、状态/进度算法和无人值守凭据方案全部确认后启用。
- 查询返回零条、多条歧义记录、外部键变化或字段缺失时，状态设为 `needs_attention`，不得猜测并覆盖业务数据。

### 11.3 数模试点建议字段映射（仍需数据验证）

现有 TDC 数模报表字段包含流水号、申请人、部门、科室、申请日期、项目车型、零件号和模型编号等查询条件，但仓库证据不足以确认哪个字段唯一代表首页“数模审批流程”。试点发现阶段应验证：

- 权威外部键：优先真实流程流水号；若一个交付物对应多条流程，需要明确聚合范围。
- 负责人：申请人、当前审批人或业务负责人中的哪一个。
- 计划完成日期：TDC 是否提供明确计划日期；不存在时不得从申请日期推算。
- 状态：TDC 审批状态到首页四种状态的映射。
- 进度：是否存在真实百分比；若只有审批节点，必须由用户批准节点权重后才能计算。
- 风险备注：允许从哪些非敏感字段生成摘要，长度和内容清理规则。

### 11.4 无人值守认证建议

推荐正式环境使用 Windows Credential Manager：

- SQLite 只保存凭据引用名称，不保存用户名、密码、Cookie 或 Token。
- 同步运行器在任务执行时从当前 Windows 用户的凭据库读取。
- 日志、审计和 API 响应只记录凭据引用是否可用，不输出凭据内容。
- Windows Task Scheduler 必须以拥有该凭据的同一用户身份运行。

受控环境变量只建议用于开发或短期试点：变量仅注入同步进程/计划任务，不写入仓库、`.env`、命令行参数、日志或 Web 页面。正式无人值守优先 Credential Manager。

### 11.5 尚待确认

1. 是否批准 `manual / automatic / hybrid` 三种模式及“人工字段优先、显式恢复自动”的冲突规则。
2. “自动更新”是否同时包含“立即同步”和 Windows Task Scheduler 无人值守定时同步。
3. 无人值守认证是否批准采用 Windows Credential Manager；受控环境变量是否仅限开发/试点。
4. 数模试点的映射发现是否允许先以只读查询和人工选择候选的方式完成。
5. 自动运行频率、失败重试次数、单次超时和数据过期阈值。
6. TDC 状态映射和进度算法需在看到真实候选字段后再次确认。

等待确认，尚未修改项目代码。
